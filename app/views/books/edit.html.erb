<h1>Edithing Book</h1>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_field :body %>

  <%= f.submit 'Update Book' %>

  <%= link_to 'show', book_path(@book.id) %>

  <p>|</p>

  <%= link_to 'back', '/books' %>

<% end %>